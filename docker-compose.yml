version: '3.8'
services:
  postgres:
    image: postgres:13
    restart: unless-stopped
    ports:
      - 5432:5432
    env_file:
      - .env

  api:
    build: .
    restart: unless-stopped
    ports:
      - 5000:5000
    env_file:
      - .env
    depends_on:
      - postgres
